version: 1
applications:
  - appRoot: book-management-front
    frontend:
      phases:
        preBuild:
          commands:
            - node -v
            - npm ci --include=dev
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - node_modules/**/*